# RecoverAI Environment Configuration

# Google Gemini API Key for Hybrid Classifier (Optional, falls back to deterministic rules if empty)
GEMINI_API_KEY=

# Razorpay Test Mode API Credentials (Optional, used for supported Test Mode operations)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development
